name: Create/update tag
on:
  push:
    branch: "master"
jobs:
  create-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          tag: "latest"
          message: "Latest release"
